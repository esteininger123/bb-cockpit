# B&B Cockpit — was geht ins öffentliche GitHub-Repo

# Standard: alles ignorieren
*

# Diese Dateien werden gepusht (für FireTV-Anzeige)
!.gitignore
!README.md
!index.html
!vercel.json

# Bilder (z.B. für Easter-Eggs wie Henry, Meilenstein-Bilder)
!*.jpg
!*.jpeg
!*.png
!*.webp
!*.gif
!*.svg
!assets/
!assets/**

# Pflege-Liste für Cockpit-Botschaften + Geburtstage (wird vom Cockpit live gelesen)
# ACHTUNG: liegt damit öffentlich auf GitHub. Keine sensiblen Daten reinschreiben.
!Cockpit-Botschaften.xlsx

# macOS-Müll explizit raus
.DS_Store
